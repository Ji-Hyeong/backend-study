server:
  port: 8082

spring:
  application:
    name: concurrency-lab
  datasource:
    url: jdbc:postgresql://localhost:5432/concurrency_lab
    username: lab
    password: lab
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

