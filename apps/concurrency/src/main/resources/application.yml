server:
  port: 8082

spring:
  application:
    name: concurrency
  datasource:
    url: jdbc:postgresql://localhost:5432/concurrency_study
    username: study
    password: study
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

